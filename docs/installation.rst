============
Installation
============

At the command line::

    $ easy_install custom_collections

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv custom_collections
    $ pip install custom_collections